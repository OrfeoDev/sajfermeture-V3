{% extends 'base_pro.html.twig' %}

{% block title %}ESPACE PRO - TABLEAU DE BORD{% endblock %}

{% block body %}
     <!-- Masthead-->
    <header class="masthead bg-primary text-white text-center">
        <!-- Flash message -->
        {% for label, messages in app.flashes %}
            <div class="text-center shadow alert alert-{{ label }} font-weight-bold">
                {% for message in messages %}
                    <p>{{ message|raw }}</p>
                {% endfor %}
            </div>
        {% endfor %}
        <div class="container d-flex align-items-center flex-column">
            <!-- Masthead Avatar Image-->
            <img class="masthead-avatar mb-5" src="/assets/img/pro/tournevis.png" alt="..." />
            <!-- Masthead Heading-->
            <h1 class="masthead-heading text-uppercase mb-0">MON ESPACE PRO</h1>

            <div class="d-flex justify-content-center mt-5">
                <a href="{{ path('app_pro_depannage') }}" class="btn btn-light btn-lg btn-block">Faire une demande de dépannage</a>
            </div>
            <div class="d-flex justify-content-center mt-5">
                <a href="{{ path('app_pro_devis_demande_devis') }}" class="btn btn-light btn-lg btn-block">Faire une demande de devis</a>
            </div>
        </div>
    </header>
{% endblock %}
